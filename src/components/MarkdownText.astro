---
import { marked } from "marked";
import type { MarkdownTextFragment } from "../graphql/__generated__/operations";

interface Props {
  data: MarkdownTextFragment;
}

const { data } = Astro.props;
---

<div class="rich-text" set:html={marked.parse(data.content)} />
